<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resume - <%= resume.name %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&display=swap');

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(to bottom, #2E3192, #1BFFFF);
            color: white;
            padding: 50px 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            padding-bottom: 20px;
        }

        h1 {
            font-size: 42px;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .contact {
            font-size: 18px;
        }

        .contact a {
            color: #FFD700;
            text-decoration: none;
            font-weight: bold;
        }

        .section {
            margin-top: 40px;
            text-align: left;
        }

        .section h2 {
            font-size: 24px;
            margin-bottom: 10px;
            border-bottom: 2px solid rgba(255, 255, 255, 0.5);
            padding-bottom: 5px;
        }

        .skills {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .skill {
            background: rgba(255, 215, 0, 0.8);
            color: #222;
            padding: 8px 15px;
            border-radius: 5px;
            font-weight: bold;
            font-size: 16px;
        }
        .projects a {
            color: #FFD700;
            text-decoration: none;
        }
        .singleProject{
            margin-bottom: 10px;
        }
        .projects a:hover {
            text-decoration: underline;
        }

        .education p {
            margin: 5px 0;
        }
    </style>
</head>
<body>

    <div class="container">
        
        <div class="header">
            <h1><%= resume.name %></h1>
            <div class="contact">
                <p>📞 <%= resume.phno %> |📌 <%= resume.city %> | 📧 <%= resume.email %></p>
                <p>🔗 <a href="<%= resume.linkdin %>">LinkedIn</a> | <a href="<%= resume.github %>">GitHub</a></p>
            </div>
        </div>

        <div class="section">
            <h2>🔥 Summary</h2>
            <p><%= resume.summary %></p>
        </div>

        <div class="section">
            <h2>🚀 Skills</h2>
            <div class="skills">
                <% resume.skills.forEach(skill => { %>
                    <span class="skill"><%= skill %></span>
                <% }) %>
            </div>
        </div>

        <div class="section projects">
            <h2>💡 Projects</h2>
            <% resume.projects.forEach(project => { %>
                <div class="singleProject">
                    <p><strong>🚀 <%= project.title %></strong> - <a href="<%= project.link %>"><%= project.link %></a></p>
                    <p>📜 <%= project.description %></p>
                    <p>🛠 Tech Stack: <%= project.tech_stack %></p>
                </div>
            <% }) %>
        </div>

        <div class="section education">
            <h2>🎓 Education</h2>
            <% resume.education.forEach(edu => { %>
                <p><strong><%= edu.college_name %></strong> (📅 <%= edu.starting %> - <%= edu.ending %>)</p>
                <p><strong><% edu.major %></strong></p>
                <p>📊 GPA: <%= edu.gpa %></p>
            <% }) %>
        </div>

    </div>

</body>
</html>
