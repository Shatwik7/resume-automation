<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resume - <%= resume.name %></title>
    <style>
        * {
            box-sizing: border-box;
        }
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 900px;
            margin: 30px auto;
            padding: 30px;
            background: white;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        h1, h2, h3 {
            color: #2c3e50;
        }
        .header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 2px solid #ddd;
        }
        .header h1 {
            margin: 5px 0;
            font-size: 32px;
        }
        .header p {
            color: #555;
            margin: 3px 0;
        }
        .section {
            margin-top: 20px;
        }
        .summary {
            font-size: 16px;
            color: #444;
            background: #ecf0f1;
            padding: 15px;
            border-left: 5px solid #3498db;
            border-radius: 5px;
        }
        .skills {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        .skill {
            background: #3498db;
            color: #fff;
            padding: 8px 12px;
            border-radius: 5px;
            font-size: 14px;
            font-weight: bold;
        }
        .grid {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 20px;
        }
        .contact {
            font-size: 14px;
            color: #333;
        }
        .projects a {
            color: #2980b9;
            text-decoration: none;
        }
        .projects a:hover {
            text-decoration: underline;
        }
        .edu, .proj {
            padding: 10px;
            border-left: 3px solid #2ecc71;
            background: #ecf0f1;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        .proj p {
            margin: 5px 0;
        }
    </style>
</head>
<body>

    <div class="container">
        
        <div class="header">
            <h1><%= resume.name %></h1>
            <p>📞 <%= resume.phno %> |📌 <%= resume.city %>| 📧 <%= resume.email %></p>
            <p>🔗 <a href="<%= resume.linkdin %>">LinkedIn</a> | <a href="<%= resume.github %>">GitHub</a></p>
        </div>

        <div class="section">
            <h2>Summary</h2>
            <p class="summary"><%= resume.summary %></p>
        </div>

        <div class="grid">
            <!-- Left Column -->
            <div>
                <div class="section">
                    <h2>Skills</h2>
                    <div class="skills">
                        <% resume.skills.forEach(skill => { %>
                            <span class="skill"><%= skill %></span>
                        <% }) %>
                    </div>
                </div>

                <div class="section">
                    <h2>Education</h2>
                    <% resume.education.forEach(edu => { %>
                        <div class="edu">
                            <h3><%= edu.college_name %></h3>
                            <h4><%= edu.major%></h4>
                            <p><strong>Duration:</strong> <%= edu.starting %> - <%= edu.ending %></p>
                            <p><strong>GPA:</strong> <%= edu.gpa %></p>
                        </div>
                    <% }) %>
                </div>
            </div>

            <!-- Right Column -->
            <div>
                <div class="section projects">
                    <h2>Projects</h2>
                    <% resume.projects.forEach(project => { %>
                        <div class="proj">
                            <h3><%= project.title %></h3>
                            <p><strong>Tech Stack:</strong> <%= project.tech_stack %></p>
                            <p><%= project.description %></p>
                            <p>🔗 <a href="<%= project.link %>"><%= project.link %></a></p>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>

    </div>

</body>
</html>
